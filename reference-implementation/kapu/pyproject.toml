[build-system]
requires=["setuptools>=68","wheel"]
build-backend="setuptools.build_meta"
[project]
name="kapu-authority-cell"
version="0.1.0"
requires-python=">=3.10"
dependencies=["jsonschema>=4.22,<5"]
[project.scripts]
kapu-cell="kapu.cli:main"
[tool.pytest.ini_options]
pythonpath=["src"]
testpaths=["tests"]
