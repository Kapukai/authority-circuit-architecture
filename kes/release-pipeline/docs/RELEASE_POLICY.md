# Release Policy

States: `working-draft`, `release-candidate`, `released`.

```text
Scope complete → Tests pass → Verification passes → Candidate packaged → Checksums recorded → Human review → Explicit authorization → Publication → Post-release verification → Rollback readiness
```

A ZIP is not, by itself, a released product.
